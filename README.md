# Simple Digital Clock C
A simple digital clock written in C

Just compile it with clang or gcc and run it from there it should work.
It only works with Linux. Windows support might get added. :)

The clock should look like something like this:
<img width="906" height="421" alt="grafik" src="https://github.com/user-attachments/assets/ce7a3639-0cc8-4f11-b955-e238fd864d1c" />

If you want to exit the program just press [ctrl]+[c] or just closte the terminal.
